# Model artifacts

Run the notebook through the **Export inference artifacts** section to generate:

- `model.pkl`
- `selected_features.json`
- `model_metadata.json`

These files are generated from the fitted model so the serialized model, feature contract, threshold, and metadata remain consistent.
